apiVersion: v1
kind: Service
metadata:
  name: postgres       # бэкенд обращается по этому имени: postgres:5432
  namespace: bulletin-board
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP      # доступен только внутри кластера
